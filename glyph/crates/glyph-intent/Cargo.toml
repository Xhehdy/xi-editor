[package]
name = "glyph-intent"
version = "0.1.0"
edition = "2021"
description = "Intent capture and routing for Glyph IDE"

[dependencies]
regex = "1"
tokio = { version = "1", features = ["sync", "time"] }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
tracing = "0.1"
