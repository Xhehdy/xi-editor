[package]
name = "cortex-graph"
version = "0.1.0"
edition = "2021"
description = "Code Knowledge Graph with SQLite persistence"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
tracing = "0.1"
